#ifndef __LIBRARY_LOADED__
#define __LIBRARY_LOADED__
#include <list>
#include "pin.H"

void library_loaded_function(IMG image, void* arg);
void library_unloaded_function(IMG image, void* arg);

#endif
